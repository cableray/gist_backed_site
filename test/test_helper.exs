ExUnit.start

# Mix.Task.run "ecto.create", ~w(-r GistBackedSite.Repo --quiet)
# Mix.Task.run "ecto.migrate", ~w(-r GistBackedSite.Repo --quiet)
# Ecto.Adapters.SQL.begin_test_transaction(GistBackedSite.Repo)

